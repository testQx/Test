adb shell top 查看IOW%

优点：非root手机，通用，获取方便
缺点：不够详细。IOW 高只能说明IO是怀疑点，是否是IO的瓶颈还需详细分析。
方案2:
参考：https://www.cnblogs.com/ggjucheng/archive/2012/01/05/2312625.html
adb shell vmstat 2 10
参数说明：
    第一个参数“2” 是采样时间间隔数，单位：s
    第二个参数“10” 是采样次数
    上述命令表示：vmstat 每2秒采集一次数据，采集10次自动结束
bi: 块设备每秒接收（IO写）的块数量，这里的块设备可以理解为系统上所有内置、外接存储设备。默认块大小是1024byte。
bo: 块设备每秒发送（IO读）的块数量。例如上图就是在读取文件的过程中，读取速度大约11496/s*1024byte/8/1024/1024 = 1.4M/s
一般bi 和 bo 都要接近0，不然就是IO 过于频繁，根据实际情况判断是否需要调整。
另：
     cs: 每秒上下文切换次数，例如我们调用系统函数，就要进行上下文切换，线程的切换，也要进程上下文切换。上下文切换是比较耗费资源的，这个值要越小越好，可以考虑调低线程或者进程的数目。
优点：非root手机，通用，获取方便
缺点：不够详细，无法精确判断是哪个进程/线程 IO过高。
方案3:
读取/proc/<pid>/io，可获取总的io读写


字段字段含义备注
rchar已经读取或将要从磁盘读取的字节数
wchar已经写入或将要写入的磁盘字节数
syscrI/O读次数次
syscwI/O写次数次
read_bytes实际从磁盘读取读取的字节数
write_bytes实际写入磁盘的字节数
cancelled因清空页面缓存而导致没有发生写操作的字节数